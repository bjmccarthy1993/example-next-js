import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // experimental: { // Note that we can only use this when using the latest canary version of Next.js
  //   ppr: "incremental",
  // },
};

export default nextConfig;
